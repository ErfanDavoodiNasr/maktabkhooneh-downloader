# Node
node_modules/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Keep lockfiles tracked — do not ignore them
# (removed package-lock.json ignore)

# OS
.DS_Store
Thumbs.db

# Output
download/
*.part
.coverage

# History / local scratch
.history/
docs/
backup/
New Text Document.txt
signup/

# Local secrets / test configs
# Keep empty config.json committed; only *.local.json is ignored.
*.local.json
.test-config.local.json

# macOS AppleDouble / IDE
._*
.idea/
